jwt:
  secret: test.test


spring:
  h2:
    console:
      enabled: true
      #path:
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
  datasource:
    url: jdbc:h2:tcp://localhost/~/jnuwiki
    username: sa
    driver-class-name: org.h2.Driver